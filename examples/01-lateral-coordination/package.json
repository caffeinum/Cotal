{
  "name": "@swarl/example-01-lateral-coordination",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "manager": "tsx src/manager.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@swarl/connector": "workspace:*",
    "@swarl/core": "workspace:*",
    "@swarl/manager": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.22.4"
  }
}
