{
  "name": "@swarl/example-02-cmux-handoff",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "manager": "tsx src/manager.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@swarl/core": "workspace:*",
    "@swarl/manager": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.22.4"
  }
}
