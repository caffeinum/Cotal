// DemoReadme — the README header loop. Uses the "park" treatment: the Cotal
// title (interlocking rings + wordmark) reveals centered with the three message
// lines, then shrinks and flies to a top-left header while the rotating-leader
// topology plays through peer-to-peer / supervised / hierarchical / hybrid.
// Seamless loop, 350f / ~11.7s. (Previously: headline lines + topology only.)

export { DemoHdrPark as DemoReadme } from "./DemoHdrPark";
